# Migration `20210310183012-initial-version`

This migration has been generated by rogatto at 3/10/2021, 3:30:12 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "public"."EnumTaskStatus" AS ENUM ('new', 'pending', 'onHold', 'ongoing', 'done')

CREATE TABLE "User" (
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "firstName" TEXT,
    "id" TEXT NOT NULL,
    "lastName" TEXT,
    "password" TEXT NOT NULL,
    "roles" TEXT[],
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "username" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Project" (
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "description" TEXT,
    "dueDate" TIMESTAMP(3),
    "email" TEXT NOT NULL,
    "id" TEXT NOT NULL,
    "issues" TEXT,
    "name" TEXT NOT NULL,
    "ownerId" TEXT NOT NULL,
    "startDate" TIMESTAMP(3),
    "updatedAt" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Task" (
    "assignedToId" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "estimation" INTEGER,
    "id" TEXT NOT NULL,
    "projectId" TEXT NOT NULL,
    "startDate" TIMESTAMP(3) NOT NULL,
    "status" "EnumTaskStatus" NOT NULL,
    "title" TEXT NOT NULL,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "User.username_unique" ON "User"("username")

ALTER TABLE "Project" ADD FOREIGN KEY("ownerId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Task" ADD FOREIGN KEY("assignedToId")REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Task" ADD FOREIGN KEY("projectId")REFERENCES "Project"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20210310183012-initial-version
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,58 @@
+datasource postgres {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  createdAt DateTime  @default(now())
+  firstName String?
+  id        String    @id @default(cuid())
+  lastName  String?
+  password  String
+  projects  Project[]
+  roles     String[]
+  tasks     Task[]
+  updatedAt DateTime  @updatedAt
+  username  String    @unique
+}
+
+model Project {
+  createdAt   DateTime  @default(now())
+  description String?
+  dueDate     DateTime?
+  email       String
+  id          String    @id @default(cuid())
+  issues      String?
+  name        String
+  owner       User      @relation(fields: [ownerId])
+  ownerId     String
+  startDate   DateTime?
+  tasks       Task[]
+  updatedAt   DateTime  @updatedAt
+}
+
+model Task {
+  assignedTo   User?          @relation(fields: [assignedToId])
+  assignedToId String?
+  createdAt    DateTime       @default(now())
+  estimation   Int?
+  id           String         @id @default(cuid())
+  project      Project        @relation(fields: [projectId])
+  projectId    String
+  startDate    DateTime
+  status       EnumTaskStatus
+  title        String
+  updatedAt    DateTime       @updatedAt
+}
+
+enum EnumTaskStatus {
+  new
+  pending
+  onHold
+  ongoing
+  done
+}
```


